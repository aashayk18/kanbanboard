import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Optionally, import your global styles
import App from './App'; // Path to your App component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
